; Exercise 1.14. Draw the tree illustrating the process generated by the count-change procedure of section 1.2.2 in making change for 11 cents. What are the orders of growth of the space and number of steps used by this process as the amount to be changed increases?
(define (count-change amount)
  (cc amount 5))
(define (cc amount kinds-of-coins)
  (cond ((= amount 0) 1)
        ((or (< amount 0) (= kinds-of-coins 0)) 0)
        (else (+ (cc amount (- kinds-of-coins 1))
                 (cc (- amount (first-denomination kinds-of-coins)) kinds-of-coins)))))
(define (first-denomination kinds-of-coins)
  (cond ((= kinds-of-coins 1) 1)
        ((= kinds-of-coins 2) 5)
        ((= kinds-of-coins 3) 10)
        ((= kinds-of-coins 4) 25)
        ((= kinds-of-coins 5) 50)))

; Diagram 1-14.png used from billthelizard.com
; 1. F(a, k) = 1 ; for a = 1
; 2. F(a, k) = 0 ; for a < 0
; 3. F(a, k) = 0 ; for k = 0
; 4. F(a, k) = F(a - 1, 1) ; for k = 1
; 5. F(a, k) = F(a, 1) + F(a - 5, 2) ; for k = 2
; 6. F(a, k) = F(a, 2) + F(a - 10, 3) ; for k = 3
; 7. F(a, k) = F(a, 3) + F(a - 25, 4) ; for k = 4
; 8. F(a, k) = F(a, 4) + F(a - 50, 5) ; for k = 5

; 1, 2, 3 are O(constant)
; 4 is O(n)
; 5 is O(n) + n/5 O(n) = (1 + n/5) O(n) = O(n^2)
; Similarly,
; 6 is O(n) + n/10 O(n^2) = O(n^3)
; 7 is O(n) + n/25 O(n^3) = O(n^4)
; 8 is O(n) + n/50 O(n^4) = O(n^5)
